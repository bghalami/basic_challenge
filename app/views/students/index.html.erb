<h1>All Students</h1>

<% @students.each do |student| %>
  <ul class="student-<%= student.id %>">
    <li><%= link_to student.name,  student_path(student) %></li>
    <%= link_to "Delete", student_path(student), method: "delete" %>
  </ul>
<% end %>
